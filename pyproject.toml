[project]
name = "kai-shared"
version = "0.1.0"
description = "Collection that are shared across the K.ai architecture"
readme = "README.md"
requires-python = "~=3.14.0"
dependencies = ["pydantic", "pydantic-settings>=2.14.1", "pyzmq>=27.1.0"]
